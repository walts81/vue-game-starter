import { prefix } from '../prefix';

export const getterTypes = {
  isAppBusy: `${prefix}is_app_busy`,
  toastMessages: `${prefix}toast_messages`,
  options: `${prefix}options`,
  activityLog: `${prefix}activity_log`,
};
