import { prefix } from '../prefix';

export const mutationTypes = {
  setIsAppBusy: `${prefix}set_is_app_busy`,
  setToasts: `${prefix}set_toasts`,
  setActivityLog: `${prefix}set_activity_log`,
  setTitle: `${prefix}set_title`,
};
