import { prefix } from '../prefix';

export const mutationTypes = {
  setPlayers: `${prefix}set_players`,
  addPlayer: `${prefix}add_player`,
  setMyplayerKey: `${prefix}set_my_player_id`,
  setGameName: `${prefix}set_game_name`,
  setGames: `${prefix}set_games`,
};
